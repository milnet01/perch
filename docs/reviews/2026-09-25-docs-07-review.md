# review-contract — docs/07-rules-engine.md — 2026-09-25

The loop log for the gate on docs/07-rules-engine.md. The document has never
carried a loop log and its standard does not require one, so the rows live
here. Genre: ADR / design doc (pinned), cap 3.

Armed by commit ab765e6: the §Apply order step 3 paragraph on a
`monitor`-only rule (PERC-0084), plus a clarifying comment in docs/03.

| Loop | Date | Lanes | Q1 | Q2 | Q3 | Verified | Fixed | Dismissed | Outcome |
|---|---|---|---|---|---|---|---|---|---|
| 1 | 2026-09-25 | 3, each holding every question | 4 | 1 | 2 | 7 | 7 | 1 | Fixed: monitor keyword resolution (§Geometry resolution 4), a disconnected output drops the decision without falling through (5, and the snap bullet in §Validation), the reducer issues the apply order, step 2 states that a desktop is passed on every call, step 3's same-output clause, `monitor` counts as an effect in §Validation, and two wrong owners (reducer → resolver). Dismissed: step 3 describes code that does not exist yet; docs-first, the code lands in the same change. Sweep: two restatements fixed (§Inputs and outputs, the snap bullet). Every lane disclosed seeing the arming commit's subject in its git snapshot. |
| 2 | 2026-09-25 | 3, each holding every question | 1 | 0 | 1 | 2 | 2 | 0 | All three lanes found both independently. Fixed: §Geometry resolution 1 said every named preset is a percentage (`center-in-place` keeps size; a user snap is its own absolute or percent), and the §Implementation pointers ADT list lacked `CenterKeepSize`. Surfaced and decided by the user: absolute pixels count from the target monitor's work-area top-left, not the whole desktop (resolution 3; docs/02 §Geometry expressions says the same). Sweep: §Implementation pointers' resolver line aligned. Code for the decision lands with PERC-0084. Lanes disclosed the loop-1 commit subject. |
| 3 | 2026-09-25 | 3, each holding every question (re-dispatched once: the first loop-3 batch was stopped before returning, its packet stale) | 0 | 3 | 1 | 4 | 3 | 0 | Cap reached (ADR cap 3). Fixed: docs/02 §Geometry expressions said `current` is the active window's output and accepted `all` (both contradict §Geometry resolution 4 and §Validation, and the code); §Apply order step 2 now passes a desktop only when the rule sets one. Filed, not fixed: rules re-apply on every title or state change against §Feedback-loop prevention — pre-existing, outside this change, PERC-0085. Final-loop own-fix share 2 of 4 (§Geometry resolution 4 and step 2, both loop-1 text); both are consequences this run exposed rather than repairs of repairs, so a calm cap. Second share: 1 of the run's 13 verified findings fell inside the armed span (ab765e6's step 3 paragraph), so the run was mostly an audit. Size 174 lines, not a split signal. Unverified open questions left: KWin's desktop-before-geometry order, and a `[snaps]` preset's own `monitor` being overridden silently by an apply-level one. |
